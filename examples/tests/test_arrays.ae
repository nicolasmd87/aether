// Test array syntax
main() {
    // Array literal (types inferred!)
    arr = [1, 2, 3, 4, 5];
    
    // Array indexing
    x = arr[0];
    y = arr[1];
    
    print(x);
    print(y);
}

